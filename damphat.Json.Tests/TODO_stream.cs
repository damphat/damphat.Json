using Xunit;

namespace damphat.Json.Tests
{
    public class TODO_stream
    {
        [Fact (Skip = "v2")]
        public void Parse_a_text_reader()
        {
            // TextReader reader = new StringReader("");
            // JSON.Parse(reader);

            Assert.True(false);
        }
    }
}